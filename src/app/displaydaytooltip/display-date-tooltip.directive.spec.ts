import { DisplayDateTooltipDirective } from './display-date-tooltip.directive';

describe('DisplayDateTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayDateTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
